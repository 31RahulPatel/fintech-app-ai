# FintechOps — Frontend

React + Vite SPA. See the [root README](../README.md) for the full project overview, environment
variables, and the [deployment guide](../DEPLOYMENT.md) for hosting instructions.

```bash
cp .env.example .env   # fill in Cognito + API Gateway values
npm install
npm run dev
```
